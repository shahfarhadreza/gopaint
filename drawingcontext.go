package main

/*
import "github.com/go-gl/gl/v2.1/gl"

func glDrawTex(x, y, w, h, tx, ty, tw, th, sx, sy float32, rect_tex, is_yv12, flip int) {
		//tx2 := tx / 2
		//ty2 := ty / 2
		//tw2 := tw / 2
		//th2 := th / 2
	if rect_tex == 0 {
		tx /= sx
		ty /= sy
		tw /= sx
		th /= sy
			//tx2 = tx
			//ty2 = ty
			//tw2 = tw
			//th2 = th
	}
	if flip == 1 {
		y += h
		h = -h
	}
	gl.Begin(gl.QUADS)
	gl.TexCoord2f(tx, ty)

	gl.Vertex2f(x, y)
	gl.TexCoord2f(tx, ty+th)

	gl.Vertex2f(x, y+h)
	gl.TexCoord2f(tx+tw, ty+th)

	gl.Vertex2f(x+w, y+h)
	gl.TexCoord2f(tx+tw, ty)

	gl.Vertex2f(x+w, y)
	gl.End()
}
*/
